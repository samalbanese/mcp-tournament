# Dependencies
node_modules/
gui/node_modules/

# Build output
dist/
gui/dist/
tsconfig.tsbuildinfo

# Local tournament runs (the demo run ships via gui/public/data/)
results/

# Legacy seed material (transformed into gui/public/data/, not part of the repo)
seed/

# Env & secrets
.env
.env.*
!.env.example

# Local artifacts (screenshots, reports)
artifacts/

# OS noise
.DS_Store
Thumbs.db
