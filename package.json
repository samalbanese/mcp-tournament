{
  "name": "mcp-tournament",
  "version": "0.1.0",
  "description": "AI model evaluation as an MCP server. Multi-judge panels, structured scoring, pluggable domains.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-tournament": "dist/cli.js"
  },
  "files": ["dist", "gui/dist/**/*", "benches"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "vitest run --config tests/vitest.config.ts --configLoader runner --pool threads",
    "test:unit": "vitest run --config tests/vitest.config.ts --configLoader runner --pool threads tests/unit",
    "test:e2e": "vitest run --config tests/vitest.config.ts --configLoader runner --pool threads tests/e2e",
    "lint": "eslint src/"
  },
  "keywords": ["mcp", "model-evaluation", "ai-benchmark", "multi-judge", "llm-eval"],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "openai": "^4.80.0",
    "zod": "^3.24.0",
    "commander": "^13.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0",
    "vitest": "^4.1.4"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
